###
# Important !!!
# Check PART variable in script check_environment.sh, which should
# point to a partition of a disk mounted in /mnt/lfs
###

IMPORTANT NOTE

queue.sh is complete for this stage. Use it.

We will collect temporary instruments under the user lfs. For this:
    - copy this directory to /home/lfs/
    - change owner
        # chown -R lfs:lfs /home/lfs/stage-1
    - log in as user lfs
        # su - lfs
        $ cd stage-1
    - run the scripts in the order STRICTLY corresponding to the list in the file
        ./queue

# vim:ft=conf
